#!/bin/bash

file1=$1
file2=$2

if [[ -z ${file1} ]]; then
	echo "Usage: the overlap of file1 and file2"
	echo -e "\t\$1: The file1"
	echo -e "\t\$2: The file2"

	exit 100
fi

sort ${file1} ${file2} | uniq -d
